package io.ruin.content.activities.lms

/**
 * Different fog sequence definitions that may be applied to the LMS activity.
 * @author Heaven
 */
enum class LMSFogSequence(val title: String, vararg regionIds: Int) {
    DEBTO_HIDEOUT("Debtor hideout")
}